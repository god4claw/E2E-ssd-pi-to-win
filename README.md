# E2E-ssd-pi-to-win

**Сетевой диск со сквозным шифрованием: SSD живёт на Raspberry Pi, буква диска появляется на Windows, а сама Pi не может прочитать ни одного вашего файла.**

Данные шифруются **на Windows, до отправки**. На Pi приезжают уже зашифрованные блоки с зашифрованными именами. Ключ есть только на клиенте. Отсюда «E2E» — end-to-end, сквозное: расшифровать может только та сторона, которая записала.

Ниже — полное описание технологии, ручная установка по шагам, **измеренные** скорости, честный список плюсов, минусов и того, что не сделано.

---

## Содержание

- [Что это даёт](#что-это-даёт)
- [Схема](#схема)
- [Почему это действительно E2E — проверка](#почему-это-действительно-e2e--проверка)
- [На чём собрано и измерено](#на-чём-собрано-и-измерено)
- [Скорость](#скорость)
- [Ручная установка](#ручная-установка)
- [Повседневная работа](#повседневная-работа)
- [Плюсы](#плюсы)
- [Минусы и ограничения](#минусы-и-ограничения)
- [Что реализовано, что не доработано, что не делали вовсе](#что-реализовано-что-не-доработано-что-не-делали-вовсе)
- [Риски и восстановление](#риски-и-восстановление)
- [Автоматическое развёртывание](#автоматическое-развёртывание)
- [Лицензия](#лицензия)

---

## Что это даёт

Обычный диск `X:` в проводнике Windows. Копируете туда файлы — они уезжают на SSD, воткнутый в Raspberry Pi под столом. Разница с любым сетевым диском в одном: **хранилище не знает, что хранит**. Украли Pi, вынули SSD, получили доступ к машине по SSH — увидят каталог из нечитаемых имён и потоки байт без структуры.

Три независимых слоя, каждый решает свою задачу:

| слой | чем сделан | что обеспечивает |
|---|---|---|
| транспорт | OpenSSH, встроенный `internal-sftp`, пользователь заперт в каталоге | доступ только по ключу, без оболочки, без выхода за пределы папки |
| шифрование | `rclone crypt` (XSalsa20-Poly1305, NaCl secretbox) | содержимое **и имена** файлов шифруются на клиенте |
| представление | `rclone mount` + WinFsp | буква диска, локальный кэш, отложенная запись |

Серверной программы нет вообще. На Pi не установлено ничего сверх штатного OpenSSH — ни Samba, ни NFS, ни веб-панели, ни самого rclone. Это принципиально: чем меньше кода слушает сеть, тем меньше есть что ломать.

---

## Схема

```
     ВАША МАШИНА (Windows)                        RASPBERRY PI
   ┌───────────────────────────┐              ┌──────────────────────┐
   │  проводник, любые програм.│              │                      │
   │            │              │              │                      │
   │            ▼              │              │                      │
   │   диск X:  (WinFsp)       │              │                      │
   │            │              │              │                      │
   │            ▼              │              │                      │
   │   rclone mount            │              │                      │
   │    ├── кэш на C: (ОТКРЫТЫЕ данные)       │                      │
   │    │                      │              │                      │
   │    ▼                      │              │                      │
   │   crypt: шифрование ◄── ключ только тут  │                      │
   │            │              │              │                      │
   │            ▼              │              │                      │
   │   sftp-клиент             │              │                      │
   └────────────┼──────────────┘              │                      │
                │                             │                      │
                │  SSH, порт 22, ключ ed25519 │                      │
                └────── гигабитный кабель ───►│ sshd + internal-sftp │
                                              │  пользователь cloud  │
                                              │  заперт в /srv/cloud │
                                              │            │         │
                                              │            ▼         │
                                              │   NVMe SSD, ext4     │
                                              │   /srv/cloud/data    │
                                              │   имена и содержимое │
                                              │   НЕЧИТАЕМЫ          │
                                              └──────────────────────┘
```

Ключевая точка: граница шифрования проходит **внутри вашей машины**. Всё, что уходит в кабель, уже зашифровано, и поверх этого ещё раз завёрнуто в SSH.

---

## Почему это действительно E2E — проверка

Не верьте на слово, проверьте руками. Записываем на диск `X:` файл с известной строкой:

```powershell
mkdir X:\_proof
"СЕКРЕТНАЯ-СТРОКА-ДЛЯ-ПРОВЕРКИ-12345" | Out-File -Encoding utf8 X:\_proof\plain.txt
```

Смотрим на Pi, что там оказалось:

```bash
f=$(sudo find /srv/cloud/data -newermt "-5 minutes" -type f | head -1)
echo "$f"                       # путь
sudo stat -c%s "$f"             # размер
sudo head -c 40 "$f" | od -c    # начало файла
sudo grep -ac "СЕКРЕТНАЯ" "$f"  # ищем открытый текст
```

Результат настоящего прогона:

```
путь:   /srv/cloud/data/娦従銿芛䘞蚠魽叡㲿/芪璤疝禉䈉費⏌㔠⺿
размер: 110 байт   (исходных данных было 62 байта)
0000000   R   C   L   O   N   E  \0  \0 230 372   { 351 027   &   E   N
0000020   K   0   ) 200 271   8   = 033   b 224   _   & 230   ! 035   x
поиск открытого текста: 0 совпадений
```

Что видно:

- **Имя каталога и имя файла** превратились в набор иероглифов — это кодировка `base32768`, ею записан результат шифрования имени.
- **Первые 8 байт** — метка формата `RCLONE\0\0`, дальше 24 байта случайной соли. То есть Pi знает только «это файл rclone», и ничего больше.
- **Накладные расходы ровно 48 байт** на файл: 32 байта заголовка плюс 16 байт кода целостности. 62 → 110.
- **Открытый текст не находится.** Ни в имени, ни в содержимом.

Целостность тоже под защитой: код Poly1305 не даст незаметно подменить байты в хранилище — расшифровка просто откажет.

---

## На чём собрано и измерено

Все цифры в этом документе сняты с этой конфигурации, а не взяты из спецификаций.

| | |
|---|---|
| Одноплатник | Raspberry Pi 5 Model B Rev 1.1, 2 ГБ ОЗУ |
| Система | Debian 13 (trixie), ядро 6.18.34, aarch64 |
| SSH | OpenSSH 10.0p2 |
| Диск | NVMe 1 ТБ (в примере — HS-SSD-FUTURE 1024G), 953.9 ГБ полезных |
| Шина диска | PCIe 5 GT/s ×1 (Gen 2 ×1) — **штатный режим Pi 5 без правки конфига** |
| Файловая система | ext4, GPT, блок 4096 Б, резерв под root снят (`-m 0`) |
| Сеть | eth0, 1000 Мбит/с, полный дуплекс; задержка в локальной сети 2 мс |
| Клиент | Windows 11 Pro 25H2 |
| rclone | v1.75.0 |
| WinFsp | 2.1.25156 |
| Занято на момент замеров | 207 ГБ из 938 ГБ (23 %) |

---

## Скорость

### Как мерилось

Каждая строка — отдельный прогон. Там, где это имеет смысл, кэши обойдены (`oflag=direct` / `iflag=direct`). Объём 0.5–2 ГБ, чтобы разгон и завершение не искажали картину.

### Слой за слоем

| что меряем | команда | результат |
|---|---|---|
| SSD на Pi, запись мимо кэша | `dd if=/dev/zero of=... bs=1M count=2048 oflag=direct conv=fsync` | **430 МБ/с** |
| SSD на Pi, чтение мимо кэша | `dd if=... of=/dev/null bs=1M iflag=direct` | **460 МБ/с** |
| канал SSH, Windows → Pi | `dd ... \| ssh pi 'dd of=/dev/null bs=1M'` | **103 МБ/с** |
| канал SSH, Pi → Windows | `ssh pi 'dd if=/dev/zero bs=1M' \| dd of=/dev/null` | **113 МБ/с** |

### Сквозной результат (шифрование + сеть + диск)

| операция | результат |
|---|---|
| загрузка 1 ГБ, `rclone copyto` на `secret:` | **99 МиБ/с** (≈104 МБ/с) |
| скачивание 1 ГБ, `rclone copyto` с `secret:` | **105 МиБ/с** (≈110 МБ/с) |
| запись 1 ГБ через диск `X:` (как видит проводник) | **65 МБ/с** |
| холодное чтение 407 МБ через `X:` (файла нет в кэше) | **108 МБ/с** |
| повторное чтение того же файла (уже в кэше на C:) | **≈2100 МБ/с** |
| 200 файлов по 64 КБ, 8 потоков | 1 с — **≈200 файлов/с** |
| 200 файлов по 64 КБ, 1 поток | 2 с — **≈100 файлов/с** |

### Выводы из цифр

1. **Шифрование бесплатно.** Голый SSH даёт 103–113 МБ/с, с шифрованием rclone — 99–105 МиБ/с. Разница в пределах погрешности: узкое место — гигабитный кабель (теоретический потолок ≈118 МБ/с), а не криптография. Pi 5 успевает шифровать быстрее, чем сеть успевает нести.
2. **Диск быстрее сети вчетверо.** 430–460 МБ/с против 110. Апгрейд SSD бессмыслен; апгрейд сети до 2.5 Гбит/с сразу даст рост.
3. **Запись через букву диска медленнее прямого копирования** (65 против 104 МБ/с). Причина: `--vfs-cache-mode full` сначала кладёт файл в кэш на C:, и только потом отправляет — вы платите двойной записью. Взамен получаете мгновенное повторное чтение (2100 МБ/с) и корректную работу программ, которые правят файл кусками.
4. **Мелкие файлы — слабое место.** 200 файлов/с при 64 КБ означает эффективные ≈12 МБ/с, в восемь раз хуже крупных. Виновата не скорость канала, а число обращений: на каждый файл нужен отдельный обмен по SFTP. Распаковывать архив прямо на `X:` — плохая идея; распакуйте локально и скопируйте папку целиком с `--transfers 8`.

---

## Ручная установка

Дальше — полный путь с нуля. Команды приведены целиком; подставьте свои значения вместо `<...>`.

### Что понадобится

- Raspberry Pi 5 (Pi 4 тоже подойдёт, но там нет NVMe — будет USB, медленнее) с блоком питания 5 В / 5 А;
- плата-адаптер NVMe (HAT) и SSD формата M.2 2280;
- microSD с Raspberry Pi OS 64-bit для самой системы;
- витая пара до роутера (Wi-Fi тоже работает, но потолок упадёт втрое-впятеро);
- машина с Windows 10/11.

---

### Часть A — Raspberry Pi

#### A1. Система и адрес

Поставьте Raspberry Pi OS (64-bit), включите SSH, обновитесь:

```bash
sudo apt update && sudo apt full-upgrade -y
sudo reboot
```

Закрепите за Pi постоянный адрес — либо резервированием по MAC в роутере, либо статикой в NetworkManager. Всё дальнейшее опирается на то, что адрес не меняется.

#### A2. Диск: разметка, файловая система, монтирование

Убедитесь, что SSD виден, и **проверьте имя устройства** — ошибка здесь стирает не тот диск:

```bash
lsblk -o NAME,SIZE,TYPE,MODEL
```

Ожидаемо увидите `nvme0n1` нужного объёма. Дальше:

```bash
sudo parted -s /dev/nvme0n1 mklabel gpt
sudo parted -s -a optimal /dev/nvme0n1 mkpart cloud ext4 1MiB 100%
sudo mkfs.ext4 -m 0 -L cloud /dev/nvme0n1p1
```

`-m 0` снимает резерв под root. На ext4 по умолчанию 5 % диска держатся в запасе для системных нужд; на диске, где лежат только данные, это впустую отнятые ~47 ГБ от терабайта.

Постоянное монтирование по UUID (имена вида `nvme0n1` могут переехать, UUID — нет):

```bash
sudo blkid /dev/nvme0n1p1          # запишите UUID
sudo mkdir -p /srv/cloud
echo 'UUID=<ВАШ-UUID> /srv/cloud ext4 defaults,noatime,nofail 0 2' | sudo tee -a /etc/fstab
sudo mount -a
df -h /srv/cloud
```

- `noatime` — не записывать время последнего чтения. Меньше лишних записей, дольше живёт SSD.
- `nofail` — если диска нет, Pi всё равно загрузится и пустит вас по SSH. Без этого флага отвалившийся SSD означает поездку за монитором и клавиатурой.

#### A3. Отдельный пользователь-хранитель

```bash
sudo adduser --system --group --home /srv/cloud --shell /usr/sbin/nologin cloud
sudo chown root:root /srv/cloud
sudo chmod 755 /srv/cloud
sudo mkdir -p /srv/cloud/data
sudo chown cloud:cloud /srv/cloud/data
sudo chmod 700 /srv/cloud/data
```

**Права здесь не косметика, а условие работы.** OpenSSH запирает пользователя в каталоге только если сам каталог и все его родители принадлежат root и не доступны на запись никому больше. Если сделать `/srv/cloud` принадлежащим `cloud` — вход просто перестанет работать, с невнятной ошибкой в журнале. Поэтому: внешняя папка root'овая, данные внутри — пользовательские.

Оболочка `nologin` означает, что этой учётной записью нельзя войти в систему — только работать с файлами.

#### A4. Ключ доступа

На **Windows** создайте отдельную пару ключей — не переиспользуйте основную:

```powershell
ssh-keygen -t ed25519 -f $env:USERPROFILE\.ssh\id_ed25519_cloud -C "cloud@win"
```

Публичную половину (`id_ed25519_cloud.pub`) перенесите на Pi:

```bash
sudo mkdir -p /etc/ssh/authorized_keys.d
printf 'restrict ssh-ed25519 AAAA<...ваш ключ...> cloud@win\n' | sudo tee /etc/ssh/authorized_keys.d/cloud
sudo chown root:root /etc/ssh/authorized_keys.d/cloud
sudo chmod 644 /etc/ssh/authorized_keys.d/cloud
```

Список ключей лежит **вне** домашней папки пользователя и принадлежит root: даже если что-то в хранилище пойдёт не так, дописать себе новый ключ через SFTP невозможно.

Префикс `restrict` перед ключом отключает сразу всё лишнее: проброс портов, агента, туннели, X11, выделение терминала.

#### A5. Правило для SSH

Создайте `/etc/ssh/sshd_config.d/50-cloud.conf`:

```
# Хранилище: SFTP-only, заперт в /srv/cloud, без шелла и без пробросов.
Match User cloud
    ChrootDirectory /srv/cloud
    ForceCommand internal-sftp -d /data
    AuthorizedKeysFile /etc/ssh/authorized_keys.d/%u
    PubkeyAuthentication yes
    PasswordAuthentication no
    KbdInteractiveAuthentication no
    AllowTcpForwarding no
    AllowAgentForwarding no
    X11Forwarding no
    PermitTunnel no
    PermitTTY no
```

Разбор построчно:

| строка | что делает |
|---|---|
| `Match User cloud` | всё ниже действует только на этого пользователя, остальных не трогает |
| `ChrootDirectory /srv/cloud` | для него корень мира — эта папка, выше не выйти |
| `ForceCommand internal-sftp -d /data` | что бы ни попросил клиент, запускается встроенный SFTP и сразу в `/data`; отдельная программа `sftp-server` не запускается вовсе |
| `AuthorizedKeysFile /etc/ssh/authorized_keys.d/%u` | список ключей вне досягаемости пользователя |
| `PasswordAuthentication no` | пароль подобрать нельзя, его просто нет |
| `AllowTcpForwarding no` и соседи | учётку нельзя превратить в туннель во внутреннюю сеть |

Проверка и применение:

```bash
sudo sshd -t && sudo systemctl reload ssh
```

`sshd -t` проверяет синтаксис. Перезагружать конфигурацию без этой проверки — способ потерять SSH на удалённой машине.

---

### Часть B — Windows

#### B1. Две программы

```powershell
winget install WinFsp.WinFsp
winget install Rclone.Rclone
```

WinFsp — то, что позволяет показать чужое хранилище буквой диска. rclone — тот, кто шифрует и говорит по SFTP.

#### B2. Отпечаток сервера

Чтобы клиент не соглашался молча на любой сервер, зафиксируйте отпечаток. На Pi:

```bash
ssh-keygen -lf /etc/ssh/ssh_host_ed25519_key.pub
```

На Windows:

```powershell
ssh-keyscan -t ed25519 <IP-ПАЙКИ> > $env:USERPROFILE\.ssh\known_hosts_cloud
```

Сверьте отпечатки глазами. Совпали — дальше; не совпали — вы говорите не с той машиной.

#### B3. Настройка rclone

Файл `%APPDATA%\rclone\rclone.conf` — два раздела: транспорт и шифрование поверх него.

```ini
[pi]
type = sftp
host = <IP-ПАЙКИ>
user = cloud
key_file = C:\Users\<ВЫ>\.ssh\id_ed25519_cloud
known_hosts_file = C:\Users\<ВЫ>\.ssh\known_hosts_cloud
shell_type = none
disable_hashcheck = true
md5sum_command = none
sha1sum_command = none

[secret]
type = crypt
remote = pi:/data
filename_encryption = standard
directory_name_encryption = true
filename_encoding = base32768
password = <сгенерируйте мастером, см. ниже>
password2 = <вторая строка, соль>
```

Пояснения к неочевидному:

- `shell_type = none`, `disable_hashcheck`, `md5sum_command = none` — обязательны. Пользователь заперт и не имеет оболочки, значит rclone не может запустить на той стороне `md5sum`. Без этих строк он будет пытаться и спотыкаться.
- `filename_encryption = standard` — шифруются и имена файлов; `directory_name_encryption = true` — и имена папок. Иначе структура каталогов останется на виду.
- `filename_encoding = base32768` — плотная запись зашифрованного имени. Обычный base32 раздувает имя примерно вдвое и упирается в предел длины гораздо раньше.

**Пароль задавайте через мастер, а не руками:**

```powershell
rclone config
```

`n` → имя `secret` → тип `crypt` → `remote` = `pi:/data` → шифрование имён `standard` → шифрование папок `yes` → пароль: `g` (сгенерировать), длина 128 → второй пароль (соль): тоже сгенерировать.

**Запишите оба пароля на бумагу и положите отдельно от компьютера.** Это не вежливая рекомендация: без них данные не восстановит никто, включая вас (см. [Риски](#риски-и-восстановление)).

#### B4. Проверка связи

```powershell
rclone lsd secret:
rclone mkdir secret:test
rclone rmdir secret:test
```

Если каталоги перечисляются — все три слоя работают.

#### B5. Монтирование

```powershell
rclone mount secret: X: `
  --vfs-cache-mode full `
  --vfs-cache-max-size 8G `
  --vfs-cache-max-age 72h `
  --vfs-write-back 5s `
  --dir-cache-time 30s `
  --volname "Cloud" `
  --transfers 8 --checkers 8 `
  --no-console `
  --log-file "$env:LOCALAPPDATA\rclone\mount.log" --log-level INFO
```

Что делает каждый рычаг:

| ключ | смысл | последствие |
|---|---|---|
| `--vfs-cache-mode full` | файл целиком проходит через локальный кэш | работают программы, которые правят файл кусками (архиваторы, редакторы, базы); ценой двойной записи |
| `--vfs-cache-max-size 8G` | потолок кэша на C: | старое вытесняется, диск не переполняется |
| `--vfs-cache-max-age 72h` | сколько держать неиспользуемое | попадание в кэш = чтение на скорости локального диска |
| `--vfs-write-back 5s` | задержка перед отправкой | сохранили в редакторе десять раз — уедет одна версия, а не десять |
| `--dir-cache-time 30s` | как долго верить списку файлов | компромисс: меньше — свежее, больше — меньше обращений |
| `--transfers 8 --checkers 8` | сколько файлов параллельно | вдвое ускоряет пачки мелких файлов (замерено: 200 против 100 файлов/с) |

---

### Часть C — автозапуск

Смонтировать при входе в систему:

```powershell
$rclone = (Get-Command rclone).Source
$arguments = 'mount secret: X: --vfs-cache-mode full --vfs-cache-max-size 8G ' +
             '--vfs-cache-max-age 72h --vfs-write-back 5s --dir-cache-time 30s ' +
             '--volname "Cloud" --transfers 8 --checkers 8 --no-console ' +
             "--log-file `"$env:LOCALAPPDATA\rclone\mount.log`" --log-level INFO"

$action  = New-ScheduledTaskAction -Execute $rclone -Argument $arguments
$trigger = New-ScheduledTaskTrigger -AtLogOn
$trigger.Delay = 'PT20S'
Register-ScheduledTask -TaskName 'RcloneCloudMount' -Action $action -Trigger $trigger -RunLevel Limited
```

Задержка в 20 секунд нужна, чтобы сеть успела подняться раньше попытки монтирования.

**Честное предупреждение.** Такое задание запускается **один раз** при входе. Если в этот момент Pi выключена или недоступна, rclone завершится с ошибкой, диска не будет, и повторных попыток не последует — придётся запускать вручную. Это реальное неудобство текущей схемы, а не гипотетическое: в журнале установки, с которой снят этот документ, такие срывы записаны 17, 19 и 20 августа. Правильное решение — в разделе [что не доработано](#что-реализовано-что-не-доработано-что-не-делали-вовсе).

---

### Часть D — приёмка

Установка считается принятой, когда выполнены все пять пунктов:

1. `rclone lsd secret:` перечисляет каталоги без ошибок;
2. диск `X:` виден в проводнике и показывает верный объём;
3. файл, записанный на `X:`, появляется на Pi под нечитаемым именем, а `grep` по открытому тексту даёт ноль совпадений (см. [проверку](#почему-это-действительно-e2e--проверка));
4. попытка войти на Pi под пользователем `cloud` с оболочкой отбивается: `ssh -i <ключ> cloud@<IP>` не даёт приглашения;
5. после перезагрузки Windows диск поднимается сам (при включённой Pi).

---

## Повседневная работа

- **Крупные файлы** — копируйте как обычно, получите около 100 МБ/с.
- **Много мелких файлов** — не распаковывайте архивы прямо на `X:`. Распакуйте локально, потом скопируйте папку целиком.
- **Проверить, всё ли доехало:** `rclone check <локальная папка> secret:<папка> --size-only`.
- **Посмотреть, что происходит:** `Get-Content $env:LOCALAPPDATA\rclone\mount.log -Tail 30 -Wait`.
- **Отмонтировать:** завершить процесс rclone (`Stop-Process -Name rclone`) — незавершённая отправка из кэша продолжится при следующем монтировании.
- **Место на диске:** `rclone about secret:` или свойства `X:` в проводнике.

---

## Плюсы

- **Хранилище не знает, что хранит.** Физический доступ к Pi или SSD ничего не даёт: имена и содержимое зашифрованы, ключ на клиенте.
- **Минимальная поверхность атаки.** На Pi слушает сеть только OpenSSH. Ни Samba с её историей уязвимостей, ни NFS, ни веб-панели, ни самого rclone.
- **Учётная запись без прав.** Без пароля, без оболочки, заперта в одной папке, без права пробрасывать порты. Даже полный перехват ключа даёт доступ только к шифротексту.
- **Шифрование ничего не стоит по скорости.** Разница между голым SSH и шифрованным rclone — в пределах погрешности; всё упирается в гигабит.
- **Нет привязки к поставщику.** Формат `rclone crypt` открыт; те же два пароля расшифруют данные любым rclone на Linux, macOS, Android — хоть напрямую с вынутого SSD.
- **Дёшево и тихо.** Около 5 Вт под нагрузкой, без вентилятора, без абонентской платы; объём ограничен только ценой SSD.
- **Прозрачно для программ.** Для Windows это обычная буква диска.

---

## Минусы и ограничения

Список честный, включая то, что мешает в реальной работе.

1. **Потолок — гигабит.** ≈110 МБ/с. SSD умеет 430–460, то есть простаивает вчетверо. Лечится сетью на 2.5 Гбит/с, а не диском.
2. **Шина диска ужата.** Pi 5 по умолчанию поднимает PCIe в режиме Gen 2 ×1. Строка `dtparam=pciex1_gen=3` в `/boot/firmware/config.txt` примерно удваивает потолок диска — но при гигабитной сети выигрыша вы не почувствуете.
3. **Мелкие файлы медленные.** ≈200 файлов/с. Каждый требует отдельного обмена по SFTP; сотня тысяч мелочи будет ползти.
4. **Очень длинные имена ломаются.** Зашифрованное имя длиннее исходного, и при глубоких путях с длинными именами (типичный пример — сохранённая целиком веб-страница) сервер отвечает `SSH_FX_BAD_MESSAGE`, файл не копируется. В журнале установки такие срывы есть. Обход — сокращать имена или паковать такие папки в архив.
5. **Кэш на C: хранит расшифрованное.** Это цена удобства: в `%LOCALAPPDATA%\rclone\vfs` лежат открытые копии недавних файлов (на момент замеров — 891 МБ). Если шифрование нужно и на самой машине — включайте BitLocker; иначе слой E2E защищает только «в пути и на Pi».
6. **Пароли в `rclone.conf` не зашифрованы, а замаскированы.** Функция `rclone obscure` обратима. Кто получил файл конфигурации — получил данные. Настоящая защита включается командой `rclone config` → `s` (set configuration password), но тогда пароль придётся вводить при каждом запуске, и автозапуск нужно переделывать.
7. **Монтирование не ждёт Pi.** Выключенная Pi = нет диска до ручного запуска.
8. **Один клиент.** Схема рассчитана на одну машину. Второй клиент с тем же ключом и паролями возможен, но одновременная запись в один файл ничем не разруливается — блокировок нет.
9. **2 ГБ ОЗУ.** Для SFTP хватает с запасом, но места для чего-то ещё на этой Pi мало.
10. **Нет резервной копии.** Один SSD, без зеркала. Шифрование защищает от чужих глаз, но не от смерти диска.
11. **Периметр открыт.** В установке, с которой снят документ: firewall не настроен, `fail2ban` не работает, вход по паролю для основного пользователя включён, а у Pi есть глобальный IPv6-адрес. Если роутер не закрывает входящие, порт 22 может быть виден из интернета. **Перед выносом такой схемы за пределы доверенной сети это надо закрыть.**
12. **Потеря паролей = потеря данных.** Без оговорок.

---

## Что реализовано, что не доработано, что не делали вовсе

### Реализовано и проверено

- [x] SSD на Pi размечен, смонтирован по UUID, переживает перезагрузку и отсутствие диска (`nofail`).
- [x] Пользователь-хранилище: без пароля, без оболочки, заперт в каталоге, ключ хранится вне его досягаемости, все пробросы отключены.
- [x] Сквозное шифрование содержимого и имён; проверено на живых данных — открытый текст в хранилище не находится.
- [x] Буква диска в Windows с локальным кэшем и отложенной записью.
- [x] Автозапуск при входе в систему.
- [x] Скорости измерены по слоям, узкое место определено (сеть).
- [x] Хранилище в работе: 207 ГБ реальных данных.

### Не доработано (работает, но с оговорками)

- [ ] **Ожидание хранилища при монтировании.** Нужен цикл повторных попыток или служба с перезапуском вместо разового задания. Сейчас — одна попытка при входе.
- [ ] **Автоперемонтирование после обрыва сети.** Диск может «зависнуть» до ручного перезапуска.
- [ ] **Длинные имена.** Нет ни предварительной проверки, ни автоматического укорачивания. Файл просто не копируется.
- [ ] **Защита конфигурации.** Пароли замаскированы, а не зашифрованы.
- [ ] **Периметр Pi.** Нет firewall, нет `fail2ban`, вход по паролю для основного пользователя не отключён, IPv6 не ограничен.
- [ ] **PCIe Gen 3.** Одна строка в конфиге, не сделана (и при гигабите не нужна).
- [ ] **Проверка целостности по расписанию.** `rclone check` есть, но никем не запускается регулярно.
- [ ] **Наблюдение за состоянием.** Никто не следит за здоровьем SSD, температурой и остатком места; о заполнении диска вы узнаете в момент отказа записи.

### Не делали вовсе

- [ ] **Доступ снаружи локальной сети.** Ни VPN, ни WireGuard, ни Tailscale. Схема живёт в пределах домашней сети.
- [ ] **Резервное копирование и зеркалирование.** Ни RAID, ни второй диск, ни выгрузка в облако — хотя выгружать шифротекст в чужое облако было бы безопасно, и это естественное развитие.
- [ ] **Версионирование и корзина.** Перезаписали файл — прошлая версия не сохраняется.
- [ ] **Многопользовательский режим.** Один ключ, один клиент, разграничения доступа нет.
- [ ] **Шифрование самого диска на Pi (LUKS).** При E2E содержимое и так закрыто, но LUKS дополнительно скрыл бы метаданные — количество, размеры и время изменения файлов, которые сейчас видны.
- [ ] **Веб-интерфейс, мобильный клиент, синхронизация папок.**
- [ ] **Ротация ключей и паролей.**
- [ ] **Монтирование до входа в систему** (как сетевой диск для служб) — сейчас диск существует только внутри вашего сеанса.

---

## Риски и восстановление

**Потеря паролей `crypt`.** Данные не восстанавливаются. Ни разработчиками rclone, ни перебором: пароль превращается в ключ через scrypt, содержимое закрыто XSalsa20-Poly1305. Единственная защита — заранее записать оба пароля (основной и соль) на бумагу и хранить отдельно от компьютера.

**Смерть SSD.** Данные теряются целиком: копии нет. Шифрование от этого не спасает — это отдельная задача, и она не решена.

**Потеря ключа SSH.** Не страшно: создайте новую пару и замените строку в `/etc/ssh/authorized_keys.d/cloud`. Данные не затрагиваются — ключ отвечает за вход, не за расшифровку.

**Кража Pi вместе с SSD.** Вор получает каталог нечитаемых имён и байты без структуры. Ему видны только метаданные: сколько файлов, какого размера, когда менялись.

**Чтение данных с вынутого SSD на другой машине.** Штатный сценарий восстановления, если Pi сгорела: подключите SSD к любому Linux, смонтируйте раздел и настройте rclone на локальный путь с теми же паролями:

```bash
rclone config create local-data local
rclone config create recover crypt remote=local-data:/mnt/ssd/data \
  filename_encryption=standard directory_name_encryption=true filename_encoding=base32768
rclone ls recover:
```

---

## Автоматическое развёртывание

Для тех, кто разворачивает это не руками, а автоматом (в том числе программным агентом), в репозитории лежит [**AGENTS.md**](AGENTS.md): те же шаги в виде проверяемого сценария — с предусловиями, идемпотентными командами, критериями приёмки, откатом и правилами обращения с секретами.

Ручной путь в этом README и сценарий в AGENTS.md описывают **одну и ту же** установку. Если они разойдутся — правильным считается README.

---

## Лицензия

[MIT](LICENSE).
